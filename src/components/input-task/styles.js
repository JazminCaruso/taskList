import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    flex: 0.95,
    borderWidth: 1,
    borderRadius: 10,
    paddingHorizontal: 10,
    color: "#212121",
    fontSize: 14,
  },
});